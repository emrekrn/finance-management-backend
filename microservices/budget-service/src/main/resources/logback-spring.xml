<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Import Spring Boot's sensible defaults and a console appender -->
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>
    <include resource="org/springframework/boot/logging/logback/console-appender.xml"/>

    <!-- If logging.pattern.console is set in application.yml, use it -->
    <springProperty scope="context" name="CONSOLE_LOG_PATTERN" source="logging.pattern.console"/>

    <!-- Application package logging level -->
    <logger name="com.emrecan.services.budget" level="INFO"/>

    <!-- Keep framework logs at reasonable level by default -->
    <logger name="org.springframework" level="INFO"/>

    <!-- Root logger -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>

    <!--
      To temporarily troubleshoot security or web issues, you may raise levels:
      <logger name="org.springframework.security" level="DEBUG"/>
      <logger name="org.springframework.web" level="DEBUG"/>
    -->
</configuration>
